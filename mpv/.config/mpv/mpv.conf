# Hardware video decoding. auto negotiates the best path
# and silently falls back to software for unsupported codecs.
# Verify with the in-player stats overlay (press `i`) -> "Hardware Decoder:" line.
hwdec=auto

# Modern GPU video output
vo=gpu-next

# Stay open on the last frame instead of quitting at end-of-file
keep-open=yes
