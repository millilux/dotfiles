# Neovim Python-PROVIDER packages ONLY — these must live in the interpreter that
# nvim calls as its python3 host (they're imported by the provider, not run as
# CLI tools). Installed via `pip3 install -r` into the mise-managed python.
#
# Standalone Python tools live elsewhere:
#   - pylsp + plugins (pyls-isort/pylsp-mypy/python-lsp-ruff/pylsp-rope)
#       -> `uv tool install python-lsp-server --with ...` in setup.sh
#   - mypy, visidata, git-when-merged   -> mise (pipx: backend)
pynvim          # neovim python host
msgpack         # msgpack-rpc for the host
ropevim         # rope refactorings in nvim (more complete than pylsp-rope, as of 2022)
rope            # backing library for ropevim
debugpy         # DAP adapter used by nvim-dap-python
